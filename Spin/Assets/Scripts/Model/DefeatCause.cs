namespace Spin.Model
{
    /// <summary>Capture is bidirectional (R11) — this records which side triggered it.</summary>
    public enum DefeatCause
    {
        SpinEnteredEnemyCell,
        EnemyEnteredSpinCell
    }
}
