namespace Spin.View
{
    /// <summary>The 6 screens of §9.3. One scene; ScreenManager swaps between these.</summary>
    public enum ScreenId
    {
        MainMenu,
        LevelMap,
        Game,
        Victory,
        Defeat,
        PuzzleComplete
    }
}
